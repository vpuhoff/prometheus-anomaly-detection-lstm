[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "prometheus-anomaly-detection-lstm"
version = "0.1.1"
description = "Prometheus time series anomaly detection with LSTM Autoencoder"
readme = "readme.md"
requires-python = ">=3.12"
authors = [{name = "Unknown", email = "unknown@example.com"}]
license = "MIT"
py_modules = [
    "cli",
    "data_collector",
    "filter_anomalous_data",
    "preprocess_data",
    "realtime_detector",
    "train_autoencoder"
]
dependencies = [
    "requests",
    "pandas",
    "pyarrow",
    "pyyaml",
    "scikit-learn",
    "joblib",
    "tensorflow",
    "matplotlib",
    "prometheus-client"
]

[project.scripts]
prometheus-ad-cli = "cli:main"

[project.urls]
Homepage = "https://github.com/example/prometheus-anomaly-detection-lstm"
